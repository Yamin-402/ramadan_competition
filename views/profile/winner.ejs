<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>لوحة المتصدرين - رمضان الملهم</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        :root {
            --primary-gradient: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%);
            --secondary-gradient: linear-gradient(45deg, #ff9966, #ff5e62);
            --islamic-green: #4CAF50;
            --gold-accent: #FFD700;
        }

        @keyframes orbit {
            0% { transform: rotate(0deg) translateX(200px) rotate(0deg); }
            100% { transform: rotate(360deg) translateX(200px) rotate(-360deg); }
        }

        @keyframes glow {
            0%, 100% { box-shadow: 0 0 15px rgba(255,215,0,0.5); }
            50% { box-shadow: 0 0 30px rgba(255,215,0,0.8); }
        }

        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-15px); }
        }

        body {
            font-family: 'Cairo', 'Tajawal', sans-serif;
            background: linear-gradient(135deg, #12c2e9, #c471ed, #f64f59);
            min-height: 100vh;
            color: white;
            overflow-x: hidden;
        }

        .leaderboard-container {
            background: rgba(255,255,255,0.1);
            backdrop-filter: blur(20px);
            border-radius: 20px;
            padding: 1.5rem;
            box-shadow: 0 20px 50px rgba(0,0,0,0.2);
            margin: 0.5rem;
        }

        /* Responsive Winner Podium */
        .winner-podium {
            display: grid;
            grid-template-columns: 1fr;
            gap: 1.5rem;
            margin-bottom: 2rem;
            position: relative;
        }

        @media (min-width: 768px) {
            .winner-podium {
                grid-template-columns: 1fr 1.5fr 1fr;
                gap: 2rem;
                align-items: flex-end;
            }
            
            .leaderboard-container {
                padding: 2rem;
                margin: 0 auto;
            }
            
            .first-place {
                transform: translateY(-30px);
            }
            
            .first-place:hover {
                transform: scale(1.05) translateY(-35px);
            }
        }

        /* Base Winner Card Styles */
        .winner-card {
            background: rgba(255, 255, 255, 0.15);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            padding: 1.25rem;
            text-align: center;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
            border: 1px solid rgba(255, 255, 255, 0.2);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
        }

        /* First Place Special Styling */
        .first-place {
            background: rgba(255, 215, 0, 0.15);
            border: 1px solid rgba(255, 215, 0, 0.4);
            z-index: 2;
            order: -1;
        }

        @media (min-width: 768px) {
            .first-place {
                order: 0;
            }
        }

        /* Profile Images */
        .profile-image {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            object-fit: cover;
            border: 5px solid;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
            transition: all 0.3s ease;
            display: block;
            margin: 0 auto;
            animation: float 3s infinite ease-in-out;
        }

        .first-place .profile-image {
            width: 150px;
            height: 150px;
            border-color: var(--gold-accent);
            animation: glow 2s infinite, float 3s infinite ease-in-out;
        }

        @media (min-width: 768px) {
            .profile-image {
                width: 140px;
                height: 140px;
            }
            
            .first-place .profile-image {
                width: 180px;
                height: 180px;
            }
        }

        .winner-card:nth-child(1) .profile-image { /* Second place */
            border-color: #c0c0c0; /* Silver */
        }

        .winner-card:nth-child(3) .profile-image { /* Third place */
            border-color: #cd7f32; /* Bronze */
            background-color: #fff; /* For UI Avatars */
        }

        /* Rank Badges */
        .rank-badge {
            position: absolute;
            top: 10px;
            right: 10px;
            background: rgba(0, 0, 0, 0.6);
            color: white;
            padding: 5px 15px;
            border-radius: 20px;
            font-weight: bold;
            font-size: 0.85rem;
            backdrop-filter: blur(5px);
        }

        .first-place .rank-badge {
            background: rgba(0, 0, 0, 0.7);
        }

        /* Text Styling */
        .winner-card h2 {
            font-size: 1.6rem;
            margin: 1rem 0 0.5rem;
            color: #fff;
            font-weight: 700;
        }

        .winner-card h3 {
            font-size: 1.4rem;
            margin: 1rem 0 0.5rem;
            color: #fff;
            font-weight: 600;
        }

        .winner-card .text-light {
            font-size: 1rem;
            color: rgba(255, 255, 255, 0.9);
            margin-bottom: 1rem;
        }

        @media (min-width: 768px) {
            .winner-card h2 {
                font-size: 1.8rem;
            }
            
            .winner-card h3 {
                font-size: 1.5rem;
            }
            
            .winner-card .text-light {
                font-size: 1.1rem;
            }
            
            .rank-badge {
                font-size: 0.9rem;
            }
        }

        /* Achievement Banners */
        .achievement-banner {
            background: rgba(0, 0, 0, 0.25);
            padding: 0.75rem;
            border-radius: 12px;
            margin-top: 1rem;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .achievement-banner h4 {
            font-size: 1rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
            color: #fff;
        }

        .first-place .achievement-banner h4 {
            color: var(--gold-accent);
        }

        .achievement-banner p {
            font-size: 0.8rem;
            color: rgba(255, 255, 255, 0.8);
            line-height: 1.4;
            margin-bottom: 0;
        }

        @media (min-width: 768px) {
            .achievement-banner {
                padding: 1rem;
            }
            
            .achievement-banner h4 {
                font-size: 1.1rem;
            }
            
            .achievement-banner p {
                font-size: 0.85rem;
            }
        }

        /* Hover Effects */
        .winner-card:hover {
            transform: scale(1.05);
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.3);
        }

        @media (min-width: 768px) {
            .winner-card:hover {
                transform: scale(1.05) translateY(-10px);
            }
        }

        /* Ramadan Stats */
        .ramadan-stats {
            display: grid;
            grid-template-columns: 1fr;
            gap: 1rem;
            margin-top: 2rem;
        }

        @media (min-width: 576px) {
            .ramadan-stats {
                grid-template-columns: repeat(2, 1fr);
            }
        }

        @media (min-width: 992px) {
            .ramadan-stats {
                grid-template-columns: repeat(3, 1fr);
            }
        }

        .stat-card {
            background: rgba(255,255,255,0.1);
            border-radius: 15px;
            padding: 1rem;
            text-align: center;
            transition: all 0.3s ease;
        }

        .stat-card:hover {
            transform: translateY(-10px);
            background: rgba(255,255,255,0.2);
        }

        .stat-card i {
            font-size: 2.5rem;
        }

        .stat-card h4 {
            font-size: 1.1rem;
            margin: 0.5rem 0;
        }

        .stat-card p {
            font-size: 1.2rem;
            font-weight: bold;
            margin: 0;
        }

        /* Progress Container */
        .progress-container {
            background: rgba(255,255,255,0.1);
            border-radius: 10px;
            padding: 1rem;
            margin-top: 2rem;
        }

        .detailed-progress {
            height: 12px;
            background: linear-gradient(to right, #4CAF50, #81c784);
            border-radius: 10px;
        }

        .quran-progress {
            background: linear-gradient(to right, #ff9966, #ff5e62);
        }

        /* Spiritual Journey Card */
        .spiritual-journey-card {
            background: rgba(255,255,255,0.1);
            border-radius: 15px;
            padding: 1rem;
            margin-top: 2rem;
            text-align: center;
        }

        .spiritual-journey-card h3 {
            font-size: 1.4rem;
            margin-bottom: 1rem;
        }

        .spiritual-journey-card p {
            margin-bottom: 1.5rem;
            font-size: 0.95rem;
            line-height: 1.5;
        }

        .celestial-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            background: linear-gradient(135deg, #12c2e9, #c471ed, #f64f59);
            opacity: 0.6;
        }

        /* Header responsive adjustments */
        .responsive-header h1 {
            font-size: 1.8rem;
        }

        .responsive-header p {
            font-size: 1rem;
        }

        @media (min-width: 576px) {
            .responsive-header h1 {
                font-size: 2.2rem;
            }
            
            .responsive-header p {
                font-size: 1.1rem;
            }
        }

        @media (min-width: 768px) {
            .responsive-header h1 {
                font-size: 2.5rem;
            }
        }
    </style>
</head>
<body>
    <div class="celestial-bg"></div>
    <div class="container py-4">
        <div class="leaderboard-container">
            <!-- Ramadan Header -->
            <div class="text-center mb-4 responsive-header">
                <h1>
                    <i class="bi bi-stars text-warning"></i> 
                    لوحة الفائزين - رمضان الكريم
                </h1>
                <p class="lead">رحلة دينية من التدني للسمو</p>
            </div>

            <!-- Winner Podium -->
            <div class="winner-podium">
                <!-- Second Place -->
                <% if (users.length >= 2) { %>
                    <div class="winner-card">
                        <div class="rank-badge">المركز الثاني</div>
                        <img src="https://ui-avatars.com/api/?name=<%= users[1].username %>&background=random" 
                            alt="<%= users[1].username %>" 
                            class="profile-image">
                        <h3 class="mt-3"><%= users[1].username %></h3>
                        <p class="text-light"><%= users[1].points.toFixed(2) %> نقطة</p>
                        <div class="achievement-banner">
                            <h4>المنافس الأصعب</h4>
                            <p>انت الي خليت للتحدي طعم, عاش</p>
                        </div>
                    </div>
                <% } %>

                <!-- First Place -->
                <% if (users.length >= 1) { %>
                    <div class="winner-card first-place">
                        <div class="rank-badge">البطل الأول</div>
                        <img src="https://ui-avatars.com/api/?name=<%= users[0].username %>&background=random" 
                            alt="<%= users[0].username %>" 
                            class="profile-image">
                        <h2 class="mt-3"><%= users[0].username %></h2>
                        <p class="text-light"><%= users[0].points.toFixed(2) %> نقطة</p>
                        <div class="achievement-banner">
                            <h4>متصدر التحدي الروحاني</h4>
                            <p>حقق أعلى مستوى من التميز في رمضان</p>
                        </div>
                    </div>
                <% } %>

                <!-- Third Place -->
                <% if (users.length >= 3) { %>
                    <div class="winner-card">
                        <div class="rank-badge">المركز الثالث</div>
                        <img src="https://ui-avatars.com/api/?name=<%= users[2].username %>&background=random" 
                            alt="<%= users[2].username %>" 
                            class="profile-image">
                        <h3 class="mt-3"><%= users[2].username %></h3>
                        <p class="text-light"><%= users[2].points.toFixed(2) %> نقطة</p>
                        <div class="achievement-banner">
                            <h4>متصدر التحدي الفاشل</h4>
                            <p>حقق أعلى مستوى من الفشل في رمضان</p>
                        </div>
                    </div>
                <% } %>
            </div>

            <!-- Ramadan Stats -->
            <div class="ramadan-stats">
                <div class="stat-card">
                    <i class="bi bi-fire text-warning display-4"></i>
                    <h4>أعلى مجموع نقاط</h4>
                    <p><%= Math.max(...users.map(u => u.points)).toFixed(2) %></p>
                </div>
                <div class="stat-card">
                    <i class="bi bi-graph-up text-success display-4"></i>
                    <h4>متوسط النقاط</h4>
                    <p><%= (users.reduce((a, b) => a + b.points, 0) / users.length).toFixed(2) %></p>
                </div>
                <div class="stat-card">
                    <i class="bi bi-people text-info display-4"></i>
                    <h4>عدد المشاركين</h4>
                    <p><%= users.length %></p>
                </div>
            </div>

            <!-- Progress Tracking -->
            <div class="progress-container">
                <h3 class="text-center mb-4">مسار التقدم</h3>
                <% users.forEach((participant, index) => { %>
                    <div class="mb-3">
                        <div class="d-flex justify-content-between">
                            <span><%= participant.username %></span>
                            <span><%= participant.points.toFixed(2) %> نقطة</span>
                        </div>
                        <div class="detailed-progress" 
                             style="width: <%= Math.min(participant.points / Math.max(...users.map(u => u.points)) * 100, 100) %>%">
                        </div>
                    </div>
                <% }) %>
            </div>

            <!-- Spiritual Journey Reflection -->
            <div class="spiritual-journey-card">
                <h3>رحلة التميز الروحاني</h3>
                <p>
                    كل نقطة تكتسبها هي خطوة نحو التطور الشخصي والديني. 
                    رمضان ليس مجرد تحدٍ، بل فرصة للارتقاء والتغيير.
                </p>
                <a href="#" class="btn btn-outline-light">استمر في مسيرتك</a>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>